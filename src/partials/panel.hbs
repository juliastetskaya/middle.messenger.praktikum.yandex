<div class="panel">
    <div class="panel__container">
        <h1 class="panel__title">{{ text }}</h1>
        <form action="#" class="panel__form">
            <div class="panel__input-block">
                {{#each fields}}
                    {{> input-field}}
                {{/each}}
            </div>
            {{> button button class="panel__button" }}
        </form>
        {{> link text=link.text href=link.href class="panel__link" }}
    </div>
</div>
